{
  "name": "moneyfromthefuture.com",
  "private": true,
  "scripts": {
    "dev": "/opt/homebrew/opt/ruby@3.3/bin/bundle exec jekyll serve --config _config.yml,_config.local.yml --host 127.0.0.1 --port 4000",
    "test": "node --test tests/*.test.mjs",
    "test:build": "node --test tests/smoke/*.test.mjs",
    "build": "/opt/homebrew/opt/ruby@3.3/bin/bundle exec jekyll build --config _config.yml",
    "verify": "npm test && npm run test:build"
  }
}
